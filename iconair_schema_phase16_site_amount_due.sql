-- ---------------------------------------------------------------------
--  Phase 16 — עלות נפרדת לכל כתובת (customer_sites.amount_due)
--
--  ללקוח רב-כתובתי (כמו אוורסט, 36 כתובות) אין כרגע שום שדה חיוב
--  ברמת הכתובת — amount_due קיים רק על customers (סכום גלובלי אחד
--  לכל הלקוח). זה מוסיף סכום ידני נפרד לכל כתובת, באותו דפוס בדיוק
--  כמו amount_due/vat_mode הקיימים על customers. vat_mode לא משוכפל
--  כאן בכוונה — הכתובת יורשת את אופן חישוב המע"מ של הלקוח שלה.
-- ---------------------------------------------------------------------

alter table public.customer_sites
  add column if not exists amount_due numeric not null default 0;
