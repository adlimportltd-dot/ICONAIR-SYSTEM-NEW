-- ---------------------------------------------------------------------
--  טיוטת סכימה — שלב 11: תאריך פירעון לכל לקוח, לדשבורד הגבייה.
--
--  ⚠ הצעה בלבד. לא רץ מעולם. דורש ששלב 9 (billing) כבר רץ.
--
--  עמודה יחידה, nullable, בלי ברירת מחדל — לקוחות קיימים פשוט לא
--  יראו תאריך (לא "מיובאים" עם תאריך שגוי) עד שתמלא אותו ידנית.
--  שום שורה קיימת לא נוגעת.
-- ---------------------------------------------------------------------

alter table public.customers
  add column if not exists payment_due_date date;
