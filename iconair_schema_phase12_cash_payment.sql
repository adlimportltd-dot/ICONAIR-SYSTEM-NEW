-- ---------------------------------------------------------------------
--  טיוטת סכימה — שלב 12: מוסיף "מזומן" כאמצעי תשלום, לצורך פילוח
--  ההכנסות בדשבורד הפיננסי (אשראי / העברה / צ'ק / מזומן / מוזמן).
--
--  ⚠ הצעה בלבד. לא רץ מעולם. דורש ששלב 9 (payment_type) כבר רץ.
--
--  רק מוסיף ערך חדש ל-enum הקיים public.payment_type — לא מוחק ולא
--  משנה אף ערך קיים (credit_card/bank_transfer/check/deferred נשארים
--  בדיוק כמו שהיו, וכל לקוח שכבר משויך אליהם לא נוגע). בדיוק כמו כל
--  הוספת ערך ל-enum בפרויקט הזה — ALTER TYPE ADD VALUE חייב לרוץ
--  כפקודה עצמאית, לא בתוך טרנזקציה שמנסה גם להשתמש בערך החדש.
-- ---------------------------------------------------------------------

alter type public.payment_type add value if not exists 'cash';
