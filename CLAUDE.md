# ICON AIR — חוקי עיצוב קבועים

מערכת שטח ל-ICON AIR (הפצת מכשירי ריח). React + Vite + Tailwind + Supabase,
ממשק עברי מלא (RTL), עיצוב **כהה תמיד** ברמת SaaS פרימיום (פחם/זהב מט —
בהשראת Vercel/Linear/Stripe, לא "ברירת מחדל של AI").

**הקובץ הזה הוא המקור היחיד לאמת על עיצוב.** כל קוד UI חדש — קומפוננטה,
מסך, מודל — חייב לעקוב אחרי החוקים כאן. אם משהו כאן סותר קוד קיים, **הקוד
הקיים טעה**, לא הקובץ הזה; תקן את הקוד, אל תוסיף חריגה.

## עקרון על: שנה ב-token, לא בקובץ

כמעט כל קומפוננטה במערכת נשענת על אותם משטחים משותפים
(`tailwind.config.js` colors/shadows/radius + `src/index.css` `.glass`,
`.inner-row`, `.chip`, `.ghost-btn`). **שינוי עיצוב רחב תמיד מתחיל שם**, לא
בעריכת עשרות קבצי קומפוננטה בנפרד — זה מה שאיפשר לכל שדרוג עיצוב קודם
בפרויקט הזה לחול על המערכת כולה מעריכת 2-3 קבצים.

## פלטת הצבעים (`tailwind.config.js` → `theme.extend.colors`)

| טוקן | ערך | תפקיד |
|---|---|---|
| `ink-950` | `#0A0A0A` | רקע הדף (`body`, `.ambient-field`) |
| `ink-900` | `#121212` | בסיס משטח הכרטיסים (`.glass`) |
| `ink-800` / `ink-700` | `#1A1A1A` / `#242424` | פנלים/הובר עמוקים יותר, שמורים לעתיד |
| `gold-300` | `#D4AF37` | זהב **בהיר** — טקסט/אייקונים/מצב פעיל על גבי הפחם |
| `gold-500` | `#C5A059` | זהב **עמוק/מט** — כפתורים, מילויים רוויים, גרדיאנטים |
| `teal-500` / `slate-500` | `#4CC9C0` / `#6E86A8` | אקצנטים משניים בגרפים/צ'יפים בלבד |
| `ok` / `warn` / `crit` / `crit-soft` | ירוק/כתום/אדום | סטטוסים בלבד — לא לקישוט |
| `text` / `text-dim` / `text-faint` | `#FFFFFF` / `#EDEDED` / `#D6D0C4` | טקסט ראשי / משני / שלישוני |

**חוק ברזל: אין צבעים חדשים.** גם לא גוונים "קרובים". צריך אקצנט נוסף?
זה סימן שצריך `tone` חדש בקומפוננטת ה-chip/status הקיימת, לא hex חדש.

## גבולות ועומק — שתי משפחות, לא מתערבבות

- **משטחי-על משותפים** (`.glass`, `.inner-row`, `.chip`, `.ghost-btn` ב-`index.css`):
  גבול בגוון זהב חלש — `border-gold-300/[0.1]` עד `border-gold-300/[0.16]`.
  זה מה שנותן לכרטיסים את הזהות המשותפת שלהם.
- **גבולות מקומיים חד-פעמיים** בתוך קומפוננטה (למשל מסגרת עדינה על מיכל
  אייקון, קו מפריד): `border-white/[0.06]` עד `border-white/[0.13]` —
  לבן שקוף על גבי פחם, לא זהב. **לעולם לא `border-white/10` בלי סוגריים
  מרובעים** — Tailwind `/10` נותן שקיפות שרירותית מהסולם הרגיל (10%),
  בעוד `/[0.075]` נותן ערך מדויק; בפרויקט הזה תמיד משתמשים בסוגריים
  מרובעים לדיוק (`border-white/[0.075]`, לא `border-white/10`).
- **בלי גבולות כבדים, אף פעם.** אם צריך להפריד אזורים ויזואלית — צללית
  או רקע שונה מעט (`bg-white/[0.02]`), לא גבול עבה או `border-2`.

עומק בונים משכבות, לא מגבול יחיד:
`shadow-lift` (הצללה חיצונית דו-שכבתית, ל"הרמת" הכרטיס מהרקע) +
`shadow-glass-hi` (הבהרה עדינה מבפנים למעלה) + לפעמים הילת רקע רדיאלית
עדינה (ר' `.glass` ב-`index.css` — `radial-gradient(...rgba(212,175,55,.05)...)`).
לאייקונים במיכל: `shadow-icon-glow`.

## מרווחים — שום דבר לא דחוס

- כרטיס (`GlassCard`): `p-6 sm:p-7`. זה כבר ברירת המחדל — אל תצמצם עם
  `!p-4` וכדומה אלא אם התוכן עצמו קטן במיוחד (למשל chip בודד).
- רשת/סקשן חיצוני בין כרטיסים: `gap-5` ומעלה (20px+), לא `gap-2`/`gap-3`.
- שורת טבלה (`DataTable`, `.inner-row`): `px-4 py-[15px]` לפחות.
- כותרת כרטיס (`CardHead`): `mb-5` מתחת לכותרת לפני התוכן.

## טיפוגרפיה

- `font-display` (Frank Ruhl Libre) — כותרות כרטיס ומספרי-על **בלבד**.
- `font-ui` (Assistant) — כל שאר הטקסט העברי. ברירת המחדל, אין צורך לציין.
- `font-mono` (IBM Plex Mono) — רק לטקסט לטיני/קוד (מק"טים, IDs), **אין**
  לו גליפים עבריים.
- מספר-על (KPI): `font-display text-[44px] font-bold tabular leading-none
  tracking-[-0.5px]`. תווית מעליו: `text-[12.5px] font-semibold uppercase
  tracking-[1.1px] text-text-faint`.
- כותרת עמודת טבלה: `text-[12px] font-semibold uppercase tracking-[0.8px]
  text-text-faint`.
- **רצפת גודל טקסט (2026-09-06, אחרי תיקון קונטרסט/גדלים בכל המערכת):**
  טקסט גוף/תווית/הינט בטפסים ומודלים — מינימום `13px`, ועדיף `14px`
  (`Field` label/input/hint, `Modal` subtitle, כפתורים — כולם כבר ב-13-14px,
  ר' `Field.jsx`). היוצא מן הכלל היחיד: שבבי סטטוס קומפקטיים (`.chip`,
  `12px`) ו-badges קטנים כמו מד-אחוזים בטבלה — אלה נשארים קומפקטיים
  בכוונה כי הם תגית/מספר בודד בתוך תא, לא טקסט לקריאה רציפה; גם הם
  לא יורדים מתחת ל-`12px`. `text-faint` (הדרג השלישי/העמום) עבר שני
  סבבי תיקון: קודם ל-`#A39B8E` (~6.8:1, "עובר AA"), ואז — כי זה עדיין
  *נתפס* כאפור דהוי בפועל, לא רק "עובר בדיקה" — ל-`#D6D0C4` (~11.4:1,
  ברמת `zinc-200`). המסקנה: ל"עובר ניגודיות טכנית" ו"נראה בהיר וברור
  לעין" יש רף שונה — כשמישהו מתלונן על טקסט דהוי, כוון לרף הגבוה, לא
  ל-AA המינימלי. אל תשתמש בגוונים אפורים נוספים/חדשים לטקסט משני, רק
  בשלושת ה-`text` tokens הקיימים.
- כל ספרה שמייצגת נתון אמיתי (לא אינדקס/מק"ט) מקבלת `tabular` (=
  `font-variant-numeric: tabular-nums`, מוגדר ב-`index.css`) — כדי
  שספרות לא "יקפצו" ברוחב כשהן מתעדכנות.

## אייקונים

כל כותרת כרטיס (`CardHead`) שיש לה קונטקסט ברור מקבלת `icon={...}` —
מרנדר מיכל קטן מואר (`h-9 w-9 rounded-xl border-gold-300/[0.16]
bg-gold-500/[0.07] shadow-icon-glow`) לצד הכותרת. אייקונים מגיעים תמיד מ-
`src/components/ui/Icons.jsx` (line icons דקים, `stroke`, לא `fill`) —
לא סמלי אימוג'י, לא ספריית אייקונים חיצונית.

## גרפים וטבלאות

- קו בגרף: עקומה חלקה (Catmull-Rom→Bezier, ר' `smoothPath` ב-
  `OilConsumptionChart.jsx`) שעוברת בדיוק דרך כל נקודת נתון — **לא**
  polyline של קווים ישרים בין נקודות.
- שטח מתחת לקו: `linearGradient` יורד מ-`stopOpacity=".34"` ל-`"0"`,
  בגוון הזהב (`gold-500`) או הטורקיז (`teal-500`) בהתאם לסדרה.
  קווי-רשת ברקע: `rgba(255,255,255,.06)` בלבד — לא רשת כבדה.
- טבלה (`DataTable`): שורות גבוהות (למעלה), הובר עדין
  (`hover:bg-white/[0.045] hover:border-gold-300/30`), לא צבע רקע קבוע
  לסירוגין (zebra striping) — זה לא בסגנון המערכת.

## RTL — לא הערת אגב, כלל מחייב

הממשק עברי-מלא. **תמיד** `text-start`/`text-end`, `ms-`/`me-`,
`ps-`/`pe-` — **אף פעם לא** `text-left`/`text-right`, `ml-`/`mr-`,
`pl-`/`pr-`. היוצא מן הכלל היחיד: תוכן שהוא במפורש LTR בכוונה (מספרי
טלפון, כתובות אימייל, קוד) מקבל `dir="ltr"` מפורש על האלמנט + עדיין
`text-start` הגיוני בהקשרו.

## מה **לא** לעשות (כבר נשקל ונדחה בכוונה)

- **לא shadcn/ui, לא Radix.** התנגש עם מערכת העיצוב הקיימת בלי תוספת
  ערך אמיתית — כבר נבדק, כבר סורב, אל תציע שוב בלי בקשה מפורשת חדשה.
- **לא לבן/בהיר.** המערכת הייתה בהירה לזמן קצר (2026-09-03) וחזרה לכהה
  באותו יום לפי בקשת המשתמש — כהה זו ברירת המחדל הקבועה.
- **לא לבנות "סטטוס" או "טריגר" לאינטגרציה שלא קיימת בפועל** (Twilio,
  SMS, WhatsApp אוטומטי, Rivhit וכו') רק כי בקשה ניסחה את זה כאילו היא
  קיימת. בדוק את הקוד קודם; אם זה לא שם, תגיד את זה במפורש במקום לבנות
  UI דקורטיבי סביב אינטגרציה בדיונית.
