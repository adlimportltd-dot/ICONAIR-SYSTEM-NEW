# ICON AIR — חוקי עיצוב קבועים

מערכת שטח ל-ICON AIR (הפצת מכשירי ריח). React + Vite + Tailwind + Supabase,
ממשק עברי מלא (RTL). עיצוב פאנל-ניהול ארגוני "high-contrast": קנבס תוכן
לבן טהור לחלוטין, טקסט שחור-כחלחל עמוק ומודגש, ו**שבב ניווט כהה** (סרגל
צד, שורה עליונה, ניווט תחתון בנייד) בנייבי-עמוק עם הדגשי ענבר/זהב עשיר.

**עדכון 2026-09-09 (Full Design Overhaul #2, "פאנל אדל"):** המערכת עברה
מ"קריסטל קריר" עדין (רקע `#F8FAFC`, הכל בהיר כולל סרגל הצד) לפלטת
ניגודיות-גבוהה: סרגל הצד/שורה עליונה/ניווט תחתון נהיו נייבי כהה (`slate-900`),
בעוד הקנבס הראשי הפך ללבן **טהור** (`#FFFFFF`, לא `slate-50`), הטקסט
לשחור-כחלחל (`slate-950`), והזהב מ"amber-gold" מאופק ל"ענבר" Tailwind
עשיר ובוהק. זו הפעם השלישית שהפלטה משתנה מהותית — **אל תניח שזו עוד
תנודה חולפת ואל תחזור אחורה מיוזמתך.** אם המשתמש מבקש שוב לשנות ערכת
נושא, בצע את הבקשה במלואה (טוקנים + כל ה-hex/rgba הספציפיים המפורטים
למטה) והחלף את הקטעים הרלוונטיים במסמך הזה, בדיוק כפי שנעשה כאן.

**הקובץ הזה הוא המקור היחיד לאמת על עיצוב.** כל קוד UI חדש — קומפוננטה,
מסך, מודל — חייב לעקוב אחרי החוקים כאן. אם משהו כאן סותר קוד קיים, **הקוד
הקיים טעה**, לא הקובץ הזה; תקן את הקוד, אל תוסיף חריגה.

## עקרון על: שנה ב-token, לא בקובץ

כמעט כל קומפוננטה במערכת נשענת על אותם משטחים משותפים
(`tailwind.config.js` colors/shadows/radius + `src/index.css` `.glass`,
`.inner-row`, `.chip`, `.ghost-btn`). **שינוי עיצוב רחב תמיד מתחיל שם**, לא
בעריכת עשרות קבצי קומפוננטה בנפרד — זה מה שאיפשר לכל שדרוג עיצוב קודם
בפרויקט הזה לחול על המערכת כולה מעריכת 2-3 קבצים.

## פלטת הצבעים (`tailwind.config.js` → `theme.extend.colors`)

**"שתי משפחות שבב" — זה הכלל הכי חשוב מ-2026-09-09/Overhaul #2: כמעט כל
האפליקציה היא "אזור תוכן בהיר" (קנבס לבן, טקסט כהה), חוץ מ-`Sidebar.jsx`/
`TopBar.jsx`/`BottomNav.jsx` שהם "שבב ניווט כהה" (רקע נייבי, טקסט בהיר).
לכל משפחה יש טוקני-טקסט/הדגשה נפרדים — אל תערבב:**
- **אזור תוכן בהיר** (הרוב המכריע של הקוד): `text`/`text-dim`/`text-faint`
  (כהים, ל-AA על לבן) ו-`gold-600` (ענבר-עמוק, ל-AA על לבן). זה מה
  שמופיע כמעט בכל קובץ קיים.
  - **`gold-600` הוא `amber-700` (`#B45309`) — כוון רק לרקע לבן/בהיר,
    לעולם לא לרקע הנייבי הכהה** (שם amber-700 כהה מדי וייבלע).
- **שבב ניווט כהה** (רק שלושת הקבצים הנ"ל): טקסט/הדגשה הם ליטרלים בהירים
  של Tailwind — `text-white`, `text-slate-300`/`text-slate-400` (משני),
  ו-`amber-400`/`amber-500` להדגשה/מצב-פעיל (לא `gold-600`, שם הוא כהה
  מדי לרקע כהה). זה חריג תיעודי מודע ל"חוק אין צבעים חדשים" — הוא
  קיים כדי לתת לרכיבי הניווט זהות "פאנל ארגוני" שונה מתוכן העמוד.

| טוקן | ערך | תפקיד |
|---|---|---|
| `ink-950` / `ink-900` | `#FFFFFF` / `#FFFFFF` | רקע הדף (`body`, `.ambient-field`) **וגם** בסיס משטח הכרטיסים (`.glass`) — **אותו לבן טהור, בכוונה** (2026-09-09/Overhaul #2: "רקע המסך הכללי לבן נקי לחלוטין"). כרטיס נבדל מהדף רק ע"י border+shadow, לא ע"י הבדל גוון — ר' "גבולות ועומק" למטה |
| `ink-800` / `ink-700` | `#F1F5F9` / `#E2E8F0` | אזורים שקועים (שדות קלט, בלוקי קוד, סטטיסטיקה) — `slate-100`/`slate-200` קרים, **לא** שמנת חמה |
| `gold-300` | `#FCD34D` (amber-300) | זהב **רווי/קישוטי** — גבולות, מילויי-רקע עדינים, אייקוני-דקורציה, קווי גרף. **לא לטקסט קטן** — ניגודיות נמוכה מדי על גבי לבן |
| `gold-500` | `#F59E0B` (amber-500) | זהב/ענבר **עשיר** — `bg-gold-500` על `PrimaryButton`/כפתורי-פעולה ראשיים, `hover:bg-amber-600`, תמיד עם `text-slate-950 font-extrabold` מעליו, לא לבן |
| `gold-600` | `#B45309` (amber-700) | זהב **עמוק-לטקסט על רקע בהיר בלבד** — ניווט פעיל בתוך תוכן, לינקים, מספרים מודגשים (סה"כ כספי וכו') שצריכים AA אמיתי. **לא על הנייבי הכהה** — ר' "שתי משפחות שבב" למעלה |
| `teal-500` / `slate-500` | `#0F766E` / `#475569` | אקצנטים משניים בגרפים/צ'יפים — עברו ניגודיות אמיתית כטקסט/גרפיקה על גבי לבן |
| `navy-900` / `navy-950` / `navy-800` | `#0F172A` / `#020617` / `#1E293B` | **הרקע היחיד בכל האפליקציה שאינו לבן** — `Sidebar`/`TopBar`/`BottomNav` בלבד (`navy-900` בסיס, `navy-950` לעומק/גרדיאנט, `navy-800` להובר/הדגשה בהירה יותר על גבי הכהה) |
| `ok` / `warn` / `crit` / `crit-soft` | `#15803D` / `#B45309` / `#B91C1C` / `#DC2626` | סטטוסים בתוכן הבהיר — גם כטקסט (KPI, chip, hover). בתוך שבב הניווט הכהה משתמשים בגרסאות `emerald-400`/`amber-400` ליטרליות במקום (ר' `TopBar.jsx` — "מקוונים") |
| `text` / `text-dim` / `text-faint` | `#020617` / `#334155` / `#64748B` | טקסט ראשי (שחור-כחלחל עמוק, `slate-950`) / משני / שלישוני — **בתוכן הבהיר בלבד**. הוחלף מגווני `stone` חמים (2026-09-09/Overhaul #1) לגווני `slate` קרים (Overhaul #2), לפי בקשה מפורשת "text-slate-950 font-bold" |

**חוק ברזל: אין צבעים חדשים.** גם לא גוונים "קרובים". צריך אקצנט נוסף?
זה סימן שצריך `tone` חדש בקומפוננטת ה-chip/status הקיימת, לא hex חדש.
היוצא מן הכלל התיעודי היחיד הוא `navy-*` (ר' למעלה) — קיים במפורש בשביל
שבב הניווט הכהה, לא לשימוש בשום מקום אחר.

**היסטוריית טון הרקע (חשוב לזכור, כי זה השתנה כמה פעמים):** כהה
(`#0A0A0A`, עד 2026-09-08) → שמנת חמה (`#F7F5F0`, קצר) → קריסטל קריר
(`#F8FAFC`, "Overhaul #1") → **לבן טהור + שבב-ניווט כהה** (`#FFFFFF` +
`navy-900`, מ-2026-09-09 "Overhaul #2", הנוכחית). **אל תחזיר לגרסה
קודמת כלשהי בלי בקשה מפורשת חדשה.**

**חוק שני, קריטי על רקע בהיר: `gold-300`/`gold-500` הם צבעי-קישוט, לא
צבעי-טקסט.** שניהם רוויים מדי לעבור ניגודיות AA כטקסט קטן על גבי
`ink-900`/`ink-950` (בערך 1.9–2.2:1). לכל טקסט/אייקון-דק שצריך להיראות
"זהב" ולהיקרא בבירור — סרייל, ניווט פעיל, מספר מודגש, הובר — משתמשים
ב-`gold-600` בלבד (בתוכן הבהיר; בשבב הכהה — `amber-400` ליטרלי).
`gold-300`/`gold-500` שמורים לגבולות, רקעים, ומילויי-SVG גדולים (donut
chart, מד אחוזים) שם השטח הגדול מפצה על הניגודיות הנמוכה.

**חוק שלישי — "אינדיקטור" זהב (badge חי, מיכל אייקון) צריך להיראות
נוכח, לא פסטלי.** לאלמנטים שהם *הודעה ויזואלית* (לא רק גבול דקורטיבי
בין תוכן) — מיכל אייקון של `CardHead`/`KpiCard`, תג "חי" ב-`TopBar` —
העוצמה המינימלית היא `border-gold-300/[0.3]` ומעלה (בתוכן הבהיר) או
`border-amber-400/30 bg-amber-500/[0.15]` (בשבב הכהה). לגבולות/הובר-תפריט
רגילים — עדיין עדין יותר, `/[0.06]`–`/[0.16]`.

## גבולות ועומק — שתי משפחות, לא מתערבבות

- **משטחי-על משותפים** (`.glass` ב-`index.css`, כלומר `GlassCard`/`Modal`/
  הפאנל הצף/`LoginScreen` וכו'): **גבול slate-300 דק וחד — `border-[#CBD5E1]`**
  (2026-09-09 Overhaul #2 — הוחמר מ-`#E2E8F0`/slate-200 כי הרקע מאחורי
  הכרטיס עכשיו לבן טהור זהה, בלי הפרש-גוון שיעזור להפריד; הגבול חייב
  לעשות את כל העבודה). רקע הכרטיס לבן טהור בלי הילת זהב; `.inner-row`
  הוא `bg-[#F1F5F9]` (slate-100) עם גבול `#E2E8F0`; `.ghost-btn` גבול
  `#CBD5E1`. **זהב/ענבר מופיע רק בכפתורים ראשיים, במצב-פעיל בניווט,
  ובאייקון/מספר-מודגש** — לא ברקעים ולא בגבולות של משטחים רגילים.
  **אל תחזיר גבול-זהב או גוון חם ל-`.glass` בלי בקשה מפורשת.**
- **שבב הניווט הכהה** (`Sidebar`/`TopBar`/`BottomNav` בלבד): גבול
  `border-white/[0.06]`–`/10` (לבן שקוף עדין על גבי נייבי, לא slate-300
  שנועד לרקע לבן) + `shadow-lift` הרגיל. זה משפחת-גבול שלישית, ספציפית
  לשבב הכהה — אל תשתמש בה בשום מקום אחר.
- `.inner-row`, `.chip`, `.ghost-btn` **עדיין** בגוון זהב חלש
  (`border-gold-300/[0.1]`–`[0.16]`) — אלה שורות/תגים *בתוך* כרטיס לבן
  שכבר מוגדר בבירור ע"י הגבול הניטרלי שלו, אז שם הזהב העדין עדיין נותן
  את הזהות המשותפת בלי להתחרות בהגדרת הכרטיס עצמו.
- **גבולות מקומיים חד-פעמיים** בתוך קומפוננטה (למשל מסגרת עדינה על מיכל
  אייקון, קו מפריד, הובר על שורה): `border-black/[0.06]` עד
  `border-black/[0.2]` — שחור שקוף על גבי לבן/שמנת. **לעולם לא
  `border-black/10` בלי סוגריים מרובעים** — Tailwind `/10` נותן שקיפות
  שרירותית מהסולם הרגיל (10%), בעוד `/[0.075]` נותן ערך מדויק; בפרויקט
  הזה תמיד משתמשים בסוגריים מרובעים לדיוק (`border-black/[0.075]`, לא
  `border-black/10`).
- **מיכל אייקון / badge "חי"** (אלמנט-הודעה, לא רק גבול דקורטיבי):
  `border-gold-300/[0.3]`+ ו-`bg-gold-500/[0.12]`+ — ר' "חוק שלישי" למעלה.
- **רקעים שקועים** (שדה קלט, בלוק קוד, תא נעול): `bg-ink-800` (שקוע קל)
  או `bg-ink-700` (שקוע יותר) — לא `bg-black/[0.3]` וכדומה; הטוקנים כבר
  מכוונים לגוון השמנת העמוק הנכון. חריג: הרקע הכהה שמעמעם את המסך מאחורי
  Modal פתוח (`bg-black/65` ב-`Modal.jsx`) נשאר שחור אמיתי בכל ערכת נושא
  — זו עמעום-מסך, לא משטח מותג.
- **בלי גבולות כבדים, אף פעם.** אם צריך להפריד אזורים ויזואלית — צללית
  או רקע שונה מעט (`bg-black/[0.02]` לניטרלי, `bg-gold-500/[0.05]` לחם
  ומזוהה עם המותג — ר' `.inner-row`), לא גבול עבה או `border-2`.

עומק בונים משכבות, לא מגבול יחיד: `shadow-lift` — צל רחב ורך בגוון
`slate-950` בשקיפות נמוכה (`rgba(2,6,23,...)`) + שכבה צמודה דקה ליישור
פינה — + `shadow-glass-hi` (הבהרה עדינה מבפנים למעלה, כמו קצה זכוכית).
לאייקונים במיכל: `shadow-icon-glow`. **2026-09-09 Overhaul #2: הועמק
מ-slate-200/50% (חלש מדי) ל-slate-950 בשקיפות נמוכה יותר**, כי הרקע
מאחורי הכרטיס עכשיו לבן טהור זהה (ר' `ink-950`/`ink-900` למעלה) — אין
יותר הפרש-גוון שיעזור להפריד כרטיס מרקע, אז הצל (יחד עם ה-border
slate-300) עושה את כל העבודה של "כרטיסייה נקייה וחדה".

## מרווחים — שום דבר לא דחוס

- כרטיס (`GlassCard`): `p-6 sm:p-7`. זה כבר ברירת המחדל — אל תצמצם עם
  `!p-4` וכדומה אלא אם התוכן עצמו קטן במיוחד (למשל chip בודד).
- רשת/סקשן חיצוני בין כרטיסים: `gap-5` ומעלה (20px+), לא `gap-2`/`gap-3`.
- שורת טבלה (`DataTable`, `.inner-row`): `px-4 py-[15px]` לפחות.
- כותרת כרטיס (`CardHead`): `mb-5` מתחת לכותרת לפני התוכן.

## טיפוגרפיה

- `font-display` (Frank Ruhl Libre) — כותרות כרטיס ומספרי-על **בלבד**.
- `font-ui` (Assistant) — כל שאר הטקסט העברי. ברירת המחדל, אין צורך לציין.
- `font-mono` (IBM Plex Mono) — רק לטקסט לטיני/קוד (מק"טים, IDs), **אין**
  לו גליפים עבריים.
- מספר-על (KPI): `font-display text-[44px] font-bold tabular leading-none
  tracking-[-0.5px]`. תווית מעליו: `text-[12.5px] font-semibold uppercase
  tracking-[1.1px] text-text-faint`.
- כותרת עמודת טבלה: `text-[12px] font-semibold uppercase tracking-[0.8px]
  text-text-faint`.
- **רצפת גודל טקסט (2026-09-06, אחרי תיקון קונטרסט/גדלים בכל המערכת):**
  טקסט גוף/תווית/הינט בטפסים ומודלים — מינימום `13px`, ועדיף `14px`
  (`Field` label/input/hint, `Modal` subtitle, כפתורים — כולם כבר ב-13-14px,
  ר' `Field.jsx`). היוצא מן הכלל היחיד: שבבי סטטוס קומפקטיים (`.chip`,
  `12px`) ו-badges קטנים כמו מד-אחוזים בטבלה — אלה נשארים קומפקטיים
  בכוונה כי הם תגית/מספר בודד בתוך תא, לא טקסט לקריאה רציפה; גם הם
  לא יורדים מתחת ל-`12px`.
- **היסטוריית `text-faint` (הדרג השלישי/העמום):** על גבי הפחם הכהה
  (עד 2026-09-08) עבר שני סבבי תיקון עד `#D6D0C4` (~11.4:1 מול `#121212`)
  כי "עובר AA טכנית" לא הספיק — המשתמש עדיין תפס אותו כדהוי. אחרי המעבר
  לבהיר הוא הוגדר מחדש כ-`#766F63` (~4.6:1 מול `#FFFFFF`). המסקנה
  מהסבב הכהה עדיין תקפה: כשמישהו מתלונן על טקסט דהוי, כוון לרף גבוה
  מ-AA המינימלי, לא רק "עובר בדיקה". אל תשתמש בגוונים אפורים/חומים
  נוספים לטקסט משני — רק בשלושת ה-`text` tokens הקיימים.
- כל ספרה שמייצגת נתון אמיתי (לא אינדקס/מק"ט) מקבלת `tabular` (=
  `font-variant-numeric: tabular-nums`, מוגדר ב-`index.css`) — כדי
  שספרות לא "יקפצו" ברוחב כשהן מתעדכנות.

## אייקונים

כל כותרת כרטיס (`CardHead`) שיש לה קונטקסט ברור מקבלת `icon={...}` —
מרנדר מיכל קטן מואר (`h-9 w-9 rounded-xl border-gold-300/[0.16]
bg-gold-500/[0.07] shadow-icon-glow`) לצד הכותרת. אייקונים מגיעים תמיד מ-
`src/components/ui/Icons.jsx` (line icons דקים, `stroke`, לא `fill`) —
לא סמלי אימוג'י, לא ספריית אייקונים חיצונית.

## גרפים וטבלאות

- קו בגרף: עקומה חלקה (Catmull-Rom→Bezier, ר' `smoothPath` ב-
  `OilConsumptionChart.jsx`) שעוברת בדיוק דרך כל נקודת נתון — **לא**
  polyline של קווים ישרים בין נקודות.
- שטח מתחת לקו: `linearGradient` יורד מ-`stopOpacity=".34"` ל-`"0"`,
  בגוון הזהב (`gold-500`) או הטורקיז (`teal-500`) בהתאם לסדרה.
  קווי-רשת ברקע: `rgba(44,42,41,.08)` בלבד (חום-שחור עדין על גבי לבן,
  היה `rgba(255,255,255,.06)` על גבי פחם) — לא רשת כבדה.
- כל צבע גרפי/SVG גולמי (לא Tailwind class) — גרדיאנטים, טבעות donut,
  נקודות-קצה, מדי-אחוזים — הועמק ב-2026-09-08 יחד עם הטוקנים
  (`#4CC9C0`→`#0F766E`, `#6E86A8`→`#475569`, `#F0555C`→`#B91C1C`,
  `#F0A43A`→`#B45309`, `#4ED9A4`→`#15803D`). אם מוסיפים גרף/SVG חדש —
  שאבו hex ישירות מהטבלה למעלה, לא מספרים "דומים" חדשים.
- טבלה (`DataTable`): שורות גבוהות (למעלה), הובר עדין וחם
  (`hover:bg-gold-500/[0.07] hover:border-gold-300/30`), לא צבע רקע קבוע
  לסירוגין (zebra striping) — זה לא בסגנון המערכת.

## RTL — לא הערת אגב, כלל מחייב

הממשק עברי-מלא. **תמיד** `text-start`/`text-end`, `ms-`/`me-`,
`ps-`/`pe-` — **אף פעם לא** `text-left`/`text-right`, `ml-`/`mr-`,
`pl-`/`pr-`. היוצא מן הכלל היחיד: תוכן שהוא במפורש LTR בכוונה (מספרי
טלפון, כתובות אימייל, קוד) מקבל `dir="ltr"` מפורש על האלמנט + עדיין
`text-start` הגיוני בהקשרו.

## מה **לא** לעשות (כבר נשקל ונדחה בכוונה)

- **לא shadcn/ui, לא Radix.** התנגש עם מערכת העיצוב הקיימת בלי תוספת
  ערך אמיתית — כבר נבדק, כבר סורב, אל תציע שוב בלי בקשה מפורשת חדשה.
- **לא כהה/פחם — בקנבס התוכן.** מ-2026-09-08 האפליקציה בהירה כברירת
  מחדל קבועה, ומ-2026-09-09 הקנבס הראשי אפילו לבן **טהור**. **אל תחזיר
  קנבס-תוכן כהה מיוזמתך** בלי בקשה מפורשת חדשה. **היוצא מן הכלל
  המכוון היחיד:** שבב הניווט (`Sidebar`/`TopBar`/`BottomNav`) *כן* כהה
  (נייבי `slate-900`) — זו החלטה מפורשת של Overhaul #2, לא תקלה, ולא
  סתירה לכלל: הקנבס עדיין לבן, רק שלושת רכיבי-הניווט האלה כהים. אל
  תרחיב את הכהה לשום מקום אחר (כרטיסים, טפסים, טבלאות) בלי בקשה חדשה.
- **לא לבנות "סטטוס" או "טריגר" לאינטגרציה שלא קיימת בפועל** (Twilio,
  SMS, WhatsApp אוטומטי, Rivhit וכו') רק כי בקשה ניסחה את זה כאילו היא
  קיימת. בדוק את הקוד קודם; אם זה לא שם, תגיד את זה במפורש במקום לבנות
  UI דקורטיבי סביב אינטגרציה בדיונית.

## כרטיס לקוח מאוחד (Customer Hub) — ארכיטקטורה, מ-2026-09-09

- **מסך, לא מודאל.** `src/components/CustomerProfile.jsx` תופס את מקום
  רשימת הלקוחות (`CustomersScreen` מרנדר אותו במקום הרשימה כש-`openCustomer`
  מוגדר). כל פעולה בו — עריכת פרטים, כתובת חדשה/עריכה/מחיקה, מכשיר
  חדש/עריכה/מחיקה, מחיר — היא **פאנל מוטבע**, לא חלון קופץ. אין לפתוח
  מודאל מתוך הכרטיס (חריג יחיד ומודע: `GenerateContractModal`, אשף
  חתימה). "לקוח חדש" הוא פאנל מוטבע מעל הרשימה (`CustomerDetailsForm`),
  ובסיום נפתח ישר כרטיס הלקוח.
- **פריסה: Vertical Stacked Flow, לא עמודות צד (מ-2026-09-09 Overhaul #2).**
  אין יותר `xl:grid-cols-[360px_...]` עם `aside` מקביל לתוכן — כל בלוק
  תופס רוחב מלא ונערם אנכית: (1) כרטיס עליון אחד שממזג את רצועת הסיכום
  הכספי (מכשירים פעילים/כתובות/עלות חודשית כוללת, `SummaryStat` +
  הטוטאל בענבר) עם פרטי הלקוח מתחתיה, (2) כתובות ומכשירים, (3) חוזים,
  (4) אזור מסוכן — כל אחד ב-`GlassCard` נפרד במלוא הרוחב. אם מוסיפים
  בלוק תוכן חדש לכרטיס — הוא מצטרף לערימה האנכית, לא נפתח בעמודה נפרדת.
- **טופס אחד לפרטי לקוח** — `CustomerDetailsForm.jsx` משמש גם ליצירה וגם
  לעריכה מוטבעת. **טופס אחד למכשיר** — `DeviceForm` (ב-`DeviceFormModal.jsx`)
  משמש מוטבע בכרטיס (יצירה/עריכה, כולל "+ הוספת כתובת חדשה" באותו
  טופס) ובעטיפת Modal רק במסך "מכשירים בשטח". אל תשכפל שדות.
- **תמחור — מקור אמת אחד** (`computeBreakdown` ב-`CustomerProfile.jsx`):
  מחיר-יחידה למכשיר = `devices.unit_price` אם הוזן ידנית, אחרת מחיר-הדגם
  של הכתובת (`customer_site_model_prices`), אחרת 0. תמיד לפני מע"מ;
  `computeVat(sum, 'excluded')` נותן לפני-מע"מ / מע"מ / כולל. ללקוח בלי
  כתובות המכשירים מתומחרים לפי `unit_price` בלבד. `customers.amount_due`
  הוא חיוב כללי ידני נפרד (שדה היסטורי, מוצג בפרטי הלקוח).
- **סנכרון רוחבי = Realtime, לא polling.** `customers`, `customer_sites`,
  `customer_site_model_prices`, `contracts` נוספו לפרסום
  `supabase_realtime` (phase18) — לפני כן מנויים עליהם לא קיבלו אירועים
  בכלל. מסך המסלולים ורשימת הלקוחות מאזינים גם ל-`customers`/
  `customer_sites`/`devices`. `useRealtime` מאחד פרצי אירועים ל-refetch
  אחד (250ms) — אל תסיר את זה, זה מה שמנע "תקיעות" בייבוא/סנכרון.
- **רצפת טיפוגרפיה חדשה (2026-09-09):** טקסט ממשק `15px` (גוף/כפתורים/
  שדות), משני `14px`, תוויות/צ'יפים `13.5px`, ולעולם לא מתחת ל-`12.5px`.
  שדות וכפתורים בגובה ≥44px (יעד אצבע בנייד). ה-KPI/כותרות נשארים
  `font-display`. אם מוסיפים קומפוננטה חדשה — התחילו מ-15px, לא מ-12.
- **משקל טקסט מודגש יותר (Overhaul #2):** כותרות כרטיס (`CardHead`),
  תוויות שדה (`Field`), ומספרי-KPI/סיכום עברו מ-`font-semibold`/`font-medium`
  ל-`font-bold`/`font-extrabold` (`text-slate-950 font-bold` לפי בקשה
  מפורשת). זה חל על **כותרות, תוויות ומספרים** — לא על טקסט-גוף/פסקאות
  רציפות (הערות, הינטים), ששם משקל רגיל נשאר קריא יותר. `PrimaryButton`
  ו-`ScreenToolbar`'s action button הם `font-extrabold` על רקע ענבר מלא.
